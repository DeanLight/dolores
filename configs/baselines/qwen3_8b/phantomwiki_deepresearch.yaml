# deepresearch on phantomwiki, Qwen3-8B — mirrors the pre-token-matched baseline run.
# No token matching: no budget, one attempt per task, default turn cap.

_compose:
- configs/base/model_qwen3_8b.yaml
- configs/base/benchmark_phantomwiki.yaml
- configs/base/prompt_phantomwiki_deepresearch.yaml
max_steps: 75
max_workers: 8
