# react on deepresearchqa, Llama-3.3-70B — mirrors the pre-token-matched baseline run.
# No token matching: no budget, one attempt per task, default turn cap.#
# NOTE: react runs deepresearchqa with no instructions today, so this config
# sets none either — it mirrors the existing run rather than inventing a prompt.

_compose:
- configs/base/model_llama3_70b.yaml
- configs/base/benchmark_deepresearchqa.yaml
agent: react
max_steps: 75
max_workers: 8
